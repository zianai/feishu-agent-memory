# Agent 记忆系统 · 端到端测试矩阵

> 运行方式：`bash skills/agent-memory/tests/e2e-test.sh`（加 `--keep` 保留测试数据供人工查看）。
> 最近一次结果：**46/46 通过（2026-08-30，真实 Base）**，测试数据自隔离（`tm*` 会话 / `ou_test_*` 用户）、
> 结束自动清理，可重复执行。

## 一、矩阵总览

### A. 写链路协议防御（judge → commit，17 项）

| 编号 | 验证点 | 断言 | 结果 |
|---|---|---|---|
| A0 | 空记忆列表 | commit 不报错，新记忆 0 条，水位正常推进 | ✓ |
| A1 | 正常写入 | 分类/重要度/置信度/状态 四字段逐项核对 | ✓ |
| A2 | 非法 category（`不存在的分类!!`） | 降级为「其他」，不报错 | ✓ |
| A3a | 重要度 99 | 截断为 10 | ✓ |
| A3b | 重要度 -5 | 截断为 1 | ✓ |
| A3c | 重要度缺失 | 默认 5 | ✓ |
| A4a | 置信度 7 | 截断为 1 | ✓ |
| A4b | 置信度 -1 | 截断为 **0**（0 是合法截断值，非缺失） | ✓ |
| A4c | 置信度缺失 | 默认 0.5（落入待确认，保守方向） | ✓ |
| A5 | 空 content | 不入库，commit 计数 0 | ✓ |
| A6 | 非法 scope（`team`） | 跳过该条 | ✓ |
| A8a/b/c | 同内容双 scope | user/agent 两表各自入库（哈希含 scope 不互撞），agent 侧 0.4 低置信落库 | ✓ |
| A14 | scope 字段缺失 | 默认 user 表 | ✓ |
| A11 | 证据原文超长 | 截断至 500 字符 | ✓ |
| A9a | 合法 conflict_with | 旧记忆状态 → archived | ✓ |
| A9b | 冲突审计链 | 旧记录「置换为」精确指向新记忆 ID | ✓ |
| A12 | --summary-file | 滚动摘要写入会话状态表 | ✓ |

### B. 读链路（recall，10 项）

| 编号 | 验证点 | 断言 | 结果 |
|---|---|---|---|
| B1a/b | 粗筛+精排协议 | 输出含 REFINE_REQUEST 与当前问题（LLM 可直接接手） | ✓ |
| B2a | 补零记忆 ID（U009） | --picked 精确命中 | ✓ |
| B2b | 无前导零（U9） | 归一化后命中同一记录 | ✓ |
| B3 | 中文逗号分隔多 ID | 正确解析多选 | ✓ |
| B4 | 缺 --user-id | 画像段给出明确提示而非报错 | ✓ |
| B5 | 不存在的会话 | 短期记忆降级文案，整链路不报错 | ✓ |
| B6a-d | Token 预算装填 | 先截断(1000字)后装填：2400 预算装入重要度最高两条，第三条起丢弃 | ✓ |
| B7a/b | 单条超长记忆 | 注入只见前 1000 字符（哨兵串被截断） | ✓ |

### C. 隔离（3 项）

| 编号 | 验证点 | 断言 | 结果 |
|---|---|---|---|
| C1 | 用户间隔离 | 用户 B 召回不到用户 A 的画像 | ✓ |
| C2 | Agent 间隔离 | 其他 Agent ID 召回不到 main 的经验 | ✓ |
| C3 | 画像跨 Agent 共享 | 换 Agent 后用户画像仍可见（按用户 ID 共享） | ✓ |

### D. 观测统计（5 项）

| 编号 | 验证点 | 断言 | 结果 |
|---|---|---|---|
| D1/D2 | stats 真库 JSON 模式 | 可解析且统计到测试记忆 | ✓ |
| D3a | 离线聚合 | total/active/archived 计数正确 | ✓ |
| D3b | ISO 时间解析 | 近 7 天只计入今天那条（`2026-08-30T10:00+08:00`） | ✓ |
| D3c | 待确认口径 | 只计 active 且置信度 <0.6 | ✓ |

### E. 幂等与恢复（4 项）

| 编号 | 验证点 | 断言 | 结果 |
|---|---|---|---|
| E1a | 同 resp 重跑 commit | 全部哈希跳过，零重复 | ✓ |
| E1b | 重跑不带摘要文件 | 旧摘要保留不被清空 | ✓ |
| E2 | 写入失败中止水位 | API 故障时响亮报错、不推水位、重跑自愈 | ✓（真实 404 故障中验证，见下） |
| E3 | setup-observability 幂等 | 视图已存在自动跳过 | ✓ |

### F. 痛点场景映射（3 项）

| 编号 | 痛点 | 验证方式 | 结果 |
|---|---|---|---|
| F1 | 「AI 每次对话都失忆」 | 记忆写入于会话 C1，在全新会话 C2 召回 | ✓ 跨会话记忆成立 |
| F2 | 「记忆是黑盒，错了没法纠」 | 模拟人在 Base 里直接改一行内容 → 召回读到修正后内容 | ✓ 透明可纠错成立 |
| F3 | 「过时信息一直阴魂不散」 | 冲突置换后旧偏好不再出现在召回（只取 active） | ✓ 记忆演化成立 |

## 二、矩阵之外（人工/环境依赖项）

- **事件触发读链路**（im.message.receive_v1）：需开发者在后台发布 bot 版本，属部署配置，不在自动化范围；
- **无人值守巡检**（memory-consolidate.sh）：依赖 LLM_CMD 配置，其 commit 部分与本矩阵共用同一条代码路径；
- **仪表盘**：`+dashboard-create` 路由故障期间无法创建，恢复后跑 `setup-observability.sh`（幂等）；
- **E2 写入失败中止**：已在 2026-08-30 真实飞书网关 404 故障中验证——首次静默失败的缺陷当日修复
  （create/update 校验 ok + 3 次退避重试 + 失败中止水位），并随之揪出 agent 表列名错误（写链路对 agent 范围从未成功过）。

## 三、首轮执行暴露的问题（2026-08-30）

首轮 38/44，6 个失败项全部定位并修复后复跑 46/46：

1. recfields 显示层 `or ''` 把合法截断值 0 折叠成空串（显示缺陷，库中数据正确）→ 已修；
2. A5 用例把"空内容条目"误写成有占位内容 → 重设计为直接断言 commit 计数；
3. A8b 子串断言夹带中间字段 → 拆分断言；
4. B2a picked 参数漏 `U` 前缀 → 修正参数；
5. B6 误解预算语义（先截断后装填，2400 可装两条）→ 修正期望；
6. F2 用 `@绝对路径` 传 JSON 被 CLI 拒绝 → 改内联（并加了更新响应校验）。

教训与收益：测试矩阵同时约束了**被测系统**和**测试本身**——所有失败先分清是产品缺陷还是用例缺陷，
本轮被测系统行为 100% 符合协议文档 `references/write-protocol.md` 的字段表约定。
