# 滚动摘要 Prompt（写链路 · 每次沉淀时重写）
# 变量：{{old_summary}} 旧摘要（首沉淀为空）；{{messages}} 本次水位后的新增消息
# 输出：纯文本摘要，≤800 字；写入「会话状态」表 滚动摘要 字段

把「旧摘要」与「新增消息」合并为一份新摘要，不超过 800 字。

## 旧摘要
{{old_summary}}

## 新增消息
{{messages}}

## 要求
必须保留：用户目标、已确认的决定、未完成事项、关键偏好与约束。
丢弃：寒暄、过程性讨论、与长期记忆重复的细节。
直接输出摘要纯文本，不要任何前后缀说明。
